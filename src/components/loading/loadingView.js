import React              from 'react'
import styles             from 'assets/styles/common.styl'

const LoadingView = () => (
  <div className={ styles.loadingTips }>正在加载，请稍候……</div>
)

export default LoadingView
